apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sidatingapp-1-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: 2306240080-sidating-be1.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sidatingapp-1-service
                port:
                  number: 80